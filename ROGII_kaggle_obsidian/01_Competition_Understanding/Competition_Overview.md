# Competition Overview

Predict missing `TVT` values for horizontal well points after the Prediction Start.

The task combines:

- well-path geometry
- known pre-PS `TVT_input`
- horizontal `GR`
- typewell `TVT`-`GR` reference curves
- robust well-level validation

The official metric is RMSE on submitted `tvt` values.
