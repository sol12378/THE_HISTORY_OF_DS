# 2026-05-25

## Plan

- Add Obsidian project-memory rules to agent instructions.
- Create the `ROGII_kaggle_obsidian/` vault structure.
- Document the data engineering strategy for ROGII.

## Do

- Updated `AGENTS.md`.
- Updated `CLAUDE.md`.
- Added `agent.md` compatibility entrypoint.
- Created Obsidian directories, index notes, templates, feature notes, CV notes, submission notes, and data engineering notes.
- Strengthened long-term data engineering design with versioned base tables, feature registry, metadata sidecars, validation gates, and migration policy.
- Ran EDA focused on target masks, GR missingness, TVT deltas, naive baselines, and fold balance.

## Check

- Obsidian now has a dedicated `10_Data_Engineering/` section.
- Agent instructions now require Obsidian updates for hypotheses, changes, experiment results, decisions, and next actions.
- Data architecture now separates stable base tables from mutable feature versions.
- Anchor baseline is strong at about 15.91 RMSE on all train target rows.
- Naive slope extrapolation is unstable and should not be used unregularized.
- GroupKFold by well should balance target-row counts.

## Act

- Next implementation step: build `train_base.parquet`, `test_base.parquet`, and `folds_group_well_v001.csv`.
- Next documentation step: update data notes after the processed base tables are generated.
- Use `train_base_v001.parquet` / `test_base_v001.parquet` naming for stable artifacts.
